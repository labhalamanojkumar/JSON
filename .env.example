# Copy this file to .env.local in development and fill values in production via Coolify UI

# MongoDB connection string (production)
MONGODB_URI=
MONGODB_DB=jsonformatter

# Admin token used for cookie/session generation
ADMIN_TOKEN=

# Optional: when set to 1 the container will generate a temporary admin token at startup
# and print it to the container logs (useful for initial setup). The generated token will
# be written to `.generated_admin_token` inside the container. This is opt-in only.
# GENERATE_ADMIN_TOKEN=1

# Optional: allow invalid TLS certs when using self-signed Mongo TLS (NOT for production)
# MONGO_TLS_ALLOW_INVALID=true

# Fallback superadmin credentials (use DB users in production - rotate these)
SUPERADMIN_USER=
SUPERADMIN_PASS=

# NODE_ENV (Coolify sets this but you can include if needed)
NODE_ENV=production

# Diagnostics endpoint (inside container) available at: GET /api/admin/diag
# Returns TCP checks for the hosts parsed from MONGODB_URI and a short Mongo ping.
